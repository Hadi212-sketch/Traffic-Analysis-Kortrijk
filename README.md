# Traffic Analysis – Group 3

Traffic Analysis project for Howest (CTAI) using Telraam traffic data, weather data, and AI models
(ARIMA & Prophet) to analyse and predict mobility patterns around campus.

## Setup

1. Create and activate virtual environment (example):
   - Windows:
     - `python -m venv .venv`
     - `.\.venv\Scripts\activate`
   - macOS/Linux:
     - `python3 -m venv .venv`
     - `source .venv/bin/activate`

2. Install dependencies:
   - `pip install -r requirements.txt`

3. Run dashboard (later in the project):
   - `cd dashboard`
   - `streamlit run app.py`
